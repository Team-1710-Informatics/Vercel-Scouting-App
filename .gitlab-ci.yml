stages:
  - sync

sync_to_github:
  stage: sync
  script:
    - git remote add github https://github.com/Team-1710-Informatics/Vercel-Scouting-App.git
    - git fetch --all
    - git push github --mirror

#This is only or CI/CD piepline between gitlab and github repository. Delete to delete connection