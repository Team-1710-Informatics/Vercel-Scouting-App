stages:
  - deploy

deploy_to_github:
  stage: deploy
  script:
    - git remote add github <your_github_repository_url>
    - git push github $CI_COMMIT_SHA:refs/heads/master
  only:
    - master  # or whichever branch you want to trigger the pipeline for

#This is only for CI/CD piepline between gitlab and github repository. Delete to delete connection